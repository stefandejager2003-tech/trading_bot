# --- Exchange & API ---
ccxt>=4.5.0
python-dotenv>=1.0.0

# --- Data Processing ---
pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Pandas TA (GitHub fork - stable working version)
git+https://github.com/MerlinR/Pandas-ta-fork.git

# --- The Brain (FAISS handled via conda, NOT pip) ---
# faiss-gpu installed separately via:
# conda install -c pytorch faiss-gpu

# --- Database & Utilities ---
SQLAlchemy>=2.0.0
tqdm>=4.66.0